<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>Search</title>
</head>
<body>
    <div class="container">
        <div class="form-container">
            <form action="/search" method="POST">
                <input type="text" name="movieTitle" placeholder="Find a movie">
                <button type="submit">Search</button>
            </form>
        </div>

        <% if(movieData) { %>
        <div class="result-container">
            <ul class="movie-data-list">
                <li><span class="bold">Title: </span><%= movieData.title %> </li>
                <li><span class="bold">Year:  </span> <%= movieData.year %></li>
                <li><span class="bold">Genres:  </span> <%= movieData.genres %></li>
                <li><span class="bold">Overview: </span> <%= movieData.overview %>  </li>

            </ul>

            <div class="image-container">
                <img src="<%= movieData.posterUrl %>" alt="">
            </div>
        </div>

        <% } %>
    </div>


</body>
</html>